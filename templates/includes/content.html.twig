<div id="content">
    {% if page.before_main %}
        <div id="before-main">
            {{ page.before_main }}
        </div>
    {% endif %}

    <main role="main" id="main" class="container">
        <div id="main-content" tabindex="-1">{# link is in html.html.twig #}

            <div class="card-holder">
                {{ page.breadcrumb }}
                {{ page.highlighted }}
                {{ page.help }}

                <div class="layout-content">
                    {{ page.content }}
                </div>{# /.layout-content #}

            </div>{# /.card-holder #}

            {% if page.content_secondary %}
                <div id="content-secondary">
                    {{ page.content_secondary }}
                </div>
            {% endif %}
        </div>{# /#main-content #}

        {% include '@material_khphotography/includes/sidebar_left.html.twig' %}
        {% include '@material_khphotography/includes/sidebar_right.html.twig' %}


    </main>{# /#main #}

    {% if page.after_main %}
        <div id="after-main">
            {{ page.after_main }}
        </div>
    {% endif %}

</div>{# /.content #}